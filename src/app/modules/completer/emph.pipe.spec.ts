/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { EmphPipe } from './emph.pipe';

describe('Pipe: Emph', () => {
  it('create an instance', () => {
    let pipe = new EmphPipe();
    expect(pipe).toBeTruthy();
  });
});
